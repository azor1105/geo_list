class HiveConstants {
  HiveConstants.init();
  static final HiveConstants _instance = HiveConstants.init();
  static HiveConstants get instance => _instance;
  // box_names
  String addressBox = "address_box";
}
